/*let frutas = ["Banana", "Maça", "Kiwi", "Uva", "Romã", "Carambola", "Morango"];

console.log(frutas.length);*/


//----------------------------------------------------------------------------------------------

// let numero = [20,5,3]
//console.log(numero.length);

//-------------------------------------------------------------------------------------------------

        // Explicação sobre o Reverse!        
/*
    .Reverse() -> reverter o nosso array

    let numero = [70, 100 , 120, 1000];
    numero.reverse();

    console.log(numero);

    -------------------------------------------------------------------------------------------------

    let nomes = [ "Valdemir", "Roberto", "Cabrito", "galo"];
    
    .push() -> Adicionar no final da lista.

    nomes.push("Ganço");

    console.log(nomes);

    -------------------------------------------------------------

    .unshift() -> Adicionar no começo da lista.

    nomes.unshift("Gabriel");

    console.log(nomes);

    ----------------------------------------------------------------

    .pop() -> Remover no final da lista.
    
    nomes.pop();
    
    console.log(nomes);

    ------------------------------------------------------------

    console.log(nomes.pop()); -> alem de remover ele retorna.

    console.log(nomes.pop());

    ------------------------------------------------------------

    console.log(nomes.splice(2,1))

    console.log(nomes);

    -------------------------------------------------------

    .shift() -> remove o inicio da lista.

    nomes.shift();
    
    ------------------------------------------------------

    let nomesAnimais = ["Safira", "Thor", "Spike", "Bob"];

    .include () -> se existe no array, se existir ele retornara true.

    console.log(nomesAnimais.includes("Bob")); -> false 

    console.log(nomesAnimais.includes("Safira")); -> true
    -------------------------------------------------------------------------------

    let valores = [30, 55, 90, 66];

    quero os valores maiores que 50

    let maiores = valores.filter(num => num > 50); Maior que 50.

    let maiores = valores.filter(num => num < 90); menor que 90.

    console log(maiores)

    -----------------------------------------------------------------------------------------

    .map() -> Definição: Metodo usado em arrays que 
    permite transformar os elementos do array original
    em novos valores criando um novo array, sem modificar o original.  

    let arr = [3, 4, 5, 6,];

    let modifiedarr = arr.map(function(element){
    return element *3;
    });
    
    console.log(modifiedarr); //[9, 12, 15, 18].

    -----------------------------------------------------------------------------------------------------------

    let alunos = [

        {nome: "Clara", nota: 10},
        {nome: "Laura", nota: 6},
        {nome: "Bryan", nota: 9}
    ];    
        let alunoNomes = alunos.map(aluno => aluno.nome);

        console.log(AlunoNomes);
    

    função |
           +
  let alunoNomesDois = Alunos.map(function(aluno){
    return aluno.nome;
  })
  
  let alunoNota = Alunos.map(aluno => aluno.nota);

  console.log(alunoNomeDois);

--------------------------------------------------------------------------------------

let nomes = ["caio", "leticia", "vini"];

let mensagem = nomes.map(itemDoArrayNomes => `ola, ${itemDoArreyNome`});

console.log(mensagem);

----------------------------------------------------------------------------------------

let nomes = ["isabelly", "emilly", "machado","guilherme" ];

let inicialNomes = nomes.map(Element => Element[0]);

console.log(inicialNomes);

let emails = [
    "Bryan@gmail.com", "gui@hotmail.com", "julia@outlook.com"
]

    let dominios = emails.map(Element => Element.split("@")[0]);
    let dominios1 = emails.map(Element => Element.split("@")[1]);

    console.log(dominios);
    console.log(dominios1);
*/

    

